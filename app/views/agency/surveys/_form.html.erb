<%= form_with(model: agency_survey, local: true) do |form| %>
  <% if agency_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agency_survey.errors.count, "error") %> prohibited this agency_survey from being saved:</h2>

      <ul>
        <% agency_survey.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
